# ══════════════════════════════════════════════════════════════
# Opium Security Configuration (.env)
# ══════════════════════════════════════════════════════════════
# Скопируйте этот файл в .env и заполните значения.
# НИКОГДА не коммитьте .env в репозиторий!

# ── JWT Secret Key ───────────────────────────────────────────
# Обязательно для продакшена. Генерация:
#   python -m security.auth --generate-key
OPIUM_SECRET_KEY=your-secret-key-here-change-me

# ── Admin Credentials ────────────────────────────────────────
# Логин администратора
OPIUM_ADMIN_USERNAME=admin

# Хеш пароля (PBKDF2-SHA256). Генерация:
#   python -m security.auth --setup
OPIUM_ADMIN_PASSWORD_HASH=

# ── Auth Settings ────────────────────────────────────────────
# Включить/выключить аутентификацию (true/false)
OPIUM_AUTH_ENABLED=true

# Время жизни токена в минутах (по умолчанию 1440 = 24 часа)
OPIUM_TOKEN_EXPIRE_MINUTES=1440

# ── Rate Limiting ────────────────────────────────────────────
# Включить rate limiting (true/false)
OPIUM_RATE_LIMIT_ENABLED=true

# Запросов в минуту per IP
OPIUM_RATE_LIMIT_PER_MINUTE=60

# ── IP Whitelist ─────────────────────────────────────────────
# Включить IP whitelist (true/false)
# ВНИМАНИЕ: если true - доступ ТОЛЬКО с указанных IP
OPIUM_IP_WHITELIST_ENABLED=false

# ── CORS Origins ─────────────────────────────────────────────
# Через запятую. Для продакшена - только ваш домен
OPIUM_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
