# Opium — Полное руководство по тестированию

**Версия:** 2.0  
**Дата:** 2026-02-16

Пошаговый гайд по тестированию **бизнес-логики**: FunPay-интеграция, выдача Steam-аккаунтов, аренды, авто-поднятие, Telegram-уведомления.  
Каждый пункт: **что делаем** → **что должно произойти** → **что проверяем**.

---

## Оглавление

1. [Подготовка к тестированию](#1-подготовка-к-тестированию)
2. [Настройка Lot Mappings (связь лотов FunPay → игры)](#2-настройка-lot-mappings)
3. [Настройка Steam-аккаунтов](#3-настройка-steam-аккаунтов)
4. [Тест: покупка лота — выдача аккаунта](#4-тест-покупка-лота--выдача-аккаунта)
5. [Тест: команды покупателя в чате](#5-тест-команды-покупателя-в-чате)
6. [Тест: истечение аренды](#6-тест-истечение-аренды)
7. [Тест: отзыв и бонусные часы](#7-тест-отзыв-и-бонусные-часы)
8. [Тест: повторная покупка (у покупателя уже есть аренда)](#8-тест-повторная-покупка)
9. [Тест: все аккаунты заняты](#9-тест-все-аккаунты-заняты)
10. [Тест: возврат средств](#10-тест-возврат-средств)
11. [Тест: заморозка игры / Steam-аккаунта](#11-тест-заморозка)
12. [Тест: смена пароля и кик сессий](#12-тест-смена-пароля-и-кик-сессий)
13. [Тест: авто-поднятие лотов (Auto Raise)](#13-тест-авто-поднятие-лотов)
14. [Тест: Telegram-бот — уведомления](#14-тест-telegram-бот--уведомления)
15. [Тест: Telegram-бот — команды и кнопки](#15-тест-telegram-бот--команды-и-кнопки)
16. [Тест: Log Watchers (Telegram)](#16-тест-log-watchers)
17. [Тест: шаблоны сообщений](#17-тест-шаблоны-сообщений)
18. [Тест: прокси для Steam-аккаунтов](#18-тест-прокси)
19. [Тест: ручное управление арендами (через UI)](#19-тест-ручное-управление-арендами)
20. [Тест: предупреждение об истечении аренды](#20-тест-предупреждение-об-истечении)
21. [Edge-кейсы и негативные сценарии](#21-edge-кейсы)
22. [Чеклист полного прогона](#22-чеклист)

---

## 1. Подготовка к тестированию

### 1.1 Что нужно для тестирования

| Компонент | Зачем |
|---|---|
| Запущенный Opium (`start.bat`) | Backend + UI |
| FunPay-аккаунт с валидным `golden_key` | Подключение к FunPay |
| Лоты на FunPay (хотя бы 1 лот аренды) | Для получения заказов |
| 1-2 Steam-аккаунта с `.maFile` | Для выдачи покупателям |
| Второй FunPay-аккаунт (покупатель) | Для покупки лотов |
| Telegram-бот (токен от @BotFather) | Для тестирования уведомлений |
| Свой Telegram ID | Для whitelist |

### 1.2 Подготовка системы

1. Запускаем: `start.bat`
2. Открываем UI: `http://localhost:3000`
3. Логинимся (если auth включён)
4. Аккаунт FunPay в таблице, state = **running**
5. Модули `steam_rent`, `telegram_bot`, `auto_raise` подключены

**Что проверяем:**
- [ ] Dashboard показывает аккаунт с зелёным индикатором
- [ ] В логах: `Runner got N event(s): InitialOrderEvent×M, InitialChatEvent×K`
- [ ] Нет ошибок в консоли

### 1.3 Настройка конфигурации Steam Rent

Через UI (страница Steam Rent → Config) или API:

| Параметр | Рекомендация для теста | Описание |
|---|---|---|
| `expiry_warning_minutes` | `5` | Предупреждение за 5 мин до конца (для быстрого теста) |
| `scheduler_interval` | `30` | Проверка истечения каждые 30 сек |

---

## 2. Настройка Lot Mappings

> Lot mapping — это связь между названием лота на FunPay и игрой в системе.
> Без правильного маппинга заказы **не будут обработаны**.

### 2.1 Как это работает

Когда покупатель покупает лот на FunPay, система получает `NewOrderEvent` с `description` (название лота).  
Система перебирает все lot mappings и ищет **первое совпадение по подстроке** (case-insensitive):

```
lot_mapping.lot_pattern.lower() in lot_name.lower()
```

Например:
- Лот на FunPay: `"CS2 аренда 24 часа с 500 играми"`
- Lot pattern: `"CS2 аренда 24"` → **совпадение** ✓

### 2.2 Настройка

**Что делаем:** в UI (Steam Rent → Lot Mappings) или API создаём маппинги.

**Пример:** У вас на FunPay есть лот `"CS2 аренда 2 часа"`:

```json
{
  "lot_pattern": "CS2 аренда 2 часа",
  "game_id": "cs2",
  "rent_minutes": 120,
  "bonus_minutes": 30,
  "min_rating_for_bonus": 4
}
```

| Поле | Значение | Описание |
|---|---|---|
| `lot_pattern` | `"CS2 аренда 2 часа"` | Подстрока для поиска в названии лота |
| `game_id` | `"cs2"` | ID игры (из раздела Games) |
| `rent_minutes` | `120` | Длительность аренды |
| `bonus_minutes` | `30` | Бонус за отзыв ≥ min_rating |
| `min_rating_for_bonus` | `4` | Минимальная оценка для бонуса (4-5 звёзд) |

**Что проверяем:**
- [ ] Маппинг создан и виден в списке
- [ ] `lot_pattern` точно совпадает с частью названия лота на FunPay
- [ ] `game_id` совпадает с ID игры, созданной в разделе Games
- [ ] Для каждого лота на FunPay есть свой маппинг (2ч, 24ч и т.д.)
- [ ] **Порядок маппингов важен** — первое совпадение побеждает

### 2.3 Типичные ошибки

- [ ] Опечатка в `lot_pattern` → заказ не обрабатывается (тихо пропускается)
- [ ] `game_id` не существует → ошибка при обработке заказа
- [ ] Два маппинга оба матчатся на один лот → берётся первый

---

## 3. Настройка Steam-аккаунтов

### 3.1 Добавление Steam-аккаунта

**Что делаем:** в UI (Steam Rent → Steam Accounts → Add) или через API добавляем аккаунт.

**Необходимые данные:**
- `login` — логин Steam
- `password` — пароль Steam
- `shared_secret` — из `.maFile` (для генерации Guard-кодов)
- `identity_secret` — из `.maFile` (для подтверждения смены пароля)
- `game_ids` — для каких игр доступен аккаунт (например `["cs2", "dota2"]`)

**Что проверяем:**
- [ ] Аккаунт появляется в списке со статусом `FREE`
- [ ] `game_ids` содержит нужные игры
- [ ] Пароль не отображается в списке (кнопка "Show password" отдельно)

### 3.2 Импорт .maFile

**Что делаем:** нажимаем Import maFile у добавленного аккаунта, загружаем файл.

**Что проверяем:**
- [ ] `shared_secret` и `identity_secret` импортированы
- [ ] Guard-код генерируется (кнопка "Guard Code" в UI)

### 3.3 Проверка Guard-кода

**Что делаем:** нажимаем "Guard Code" у Steam-аккаунта.

**Что проверяем:**
- [ ] 5-символьный код (буквы + цифры)
- [ ] Код меняется каждые 30 секунд
- [ ] Код работает при входе в Steam (проверьте вручную)

### 3.4 Настройки аккаунта

| Настройка | Значение | Описание |
|---|---|---|
| `change_password_on_rent` | `true` | Менять пароль при выдаче и при завершении аренды |
| `kick_devices_on_rent` | `true` | Кикать все сессии при завершении аренды |
| `frozen` | `false` | Заморозка — не выдавать аккаунт |

---

## 4. Тест: покупка лота — выдача аккаунта

> **Это главный флоу системы.** Проверяем полную цепочку: покупка → выдача данных → аренда.

### 4.1 Подготовка

- [ ] Есть хотя бы 1 игра в Games с `game_id` (например `cs2`)
- [ ] Есть хотя бы 1 Steam-аккаунт с `status: FREE`, `frozen: false`, и `game_ids` содержит `cs2`
- [ ] Есть lot mapping для лота на FunPay → `game_id: cs2`
- [ ] Игра `cs2` не заморожена (`frozen: false`)

### 4.2 Действие

**Что делаем:** со второго FunPay-аккаунта (покупатель) покупаем лот аренды.

### 4.3 Ожидаемая цепочка событий

```
1. Покупатель оплачивает лот на FunPay
      ↓
2. FunPay → NewOrderEvent (order_id, lot_name, buyer_id)
      ↓
3. Система матчит lot_name по lot_mappings → находит game_id + rent_minutes
      ↓
4. Система ищет свободный Steam-аккаунт для game_id
      ↓
5. Создаёт Rental (status: ACTIVE, delivery_pending: true)
      ↓
6. Steam-аккаунт → status: RENTED
      ↓
7. FunPay отправляет системное сообщение в чат (о покупке)
      ↓
8. Система видит NewMessageEvent → проверяет delivery_pending
      ↓
9. Генерирует Steam Guard код (если есть maFile)
      ↓
10. Отправляет покупателю в чат FunPay:
```

**Сообщение покупателю:**
```
🎮 Аренда оформлена!

Игра: CS2
Логин: steam_user42
Пароль: Ab3kQ7mN9xZw
Steam Guard: 7K3M9
Осталось: 2ч 0м
До: 16.02.2026 18:30

Команды:
!данные - данные аккаунта
!код - Steam Guard код
```

### 4.4 Что проверяем

**В логах backend:**
- [ ] `[steam_rent] New order ORDER_ID matched lot mapping → game cs2, 120 min`
- [ ] `[steam_rent] Allocated account steam_user42 for rental RENTAL_UUID`
- [ ] `[steam_rent] Delivered credentials to buyer BUYER_ID in chat CHAT_ID`

**На FunPay (чат покупателя):**
- [ ] Покупатель получил сообщение с логином, паролем, Guard-кодом
- [ ] Время аренды корректное (2ч 0м, если лот на 2 часа)
- [ ] Дата окончания верная

**В UI Opium:**
- [ ] Steam Rent → Rentals → появилась новая аренда (status: ACTIVE)
- [ ] Steam Rent → Steam Accounts → аккаунт в статусе `RENTED`
- [ ] Orders → новый заказ с тегом модуля steam_rent

**В Steam (ручная проверка):**
- [ ] Покупатель может войти с полученным логином/паролем
- [ ] Guard-код работает

### 4.5 Если что-то не работает

| Проблема | Причина | Решение |
|---|---|---|
| Заказ пропущен (нет лога) | Lot mapping не совпал | Проверить `lot_pattern` vs реальное название лота |
| "No free accounts" | Все аккаунты заняты или заморожены | Добавить свободный аккаунт или разморозить |
| Данные не отправились | `delivery_pending: true` но нет сообщения | Покупатель должен написать что-нибудь в чат (или подождать системное сообщение) |
| Guard-код пустой | Нет `.maFile` / `shared_secret` | Импортировать `.maFile` |

---

## 5. Тест: команды покупателя в чате

> Покупатель может писать команды в чат FunPay. Система отвечает автоматически.

### 5.1 Команда `!данные` (или `!аккаунт`, `!account`)

**Что делаем:** покупатель пишет `!данные` в чат FunPay.

**Ожидаемый ответ:**
```
📋 Ваши активные аренды:

🎮 CS2
Логин: steam_user42
Пароль: Ab3kQ7mN9xZw  
Steam Guard: 8N2WP
Осталось: 1ч 45м
До: 16.02.2026 18:30
```

**Что проверяем:**
- [ ] Ответ приходит автоматически в чат покупателя
- [ ] Guard-код свежий (не тот, что при выдаче — он обновляется)
- [ ] Если несколько аренд — все отображаются
- [ ] Без активных аренд → `"У вас нет активных аренд"`

### 5.2 Команда `!код` (или `!code`, `!guard`)

**Что делаем:** покупатель пишет `!код`.

**Ожидаемый ответ:**
```
🔑 Steam Guard коды:

CS2 (steam_user42): 8N2WP
```

**Что проверяем:**
- [ ] Только Guard-код (без пароля/логина) — для быстрого доступа
- [ ] Если несколько аренд → коды для всех
- [ ] Без `.maFile` → `—` вместо кода

### 5.3 Команда `!статус CS2` (или `!status`)

**Что делаем:** покупатель пишет `!статус cs2`.

**Ожидаемый ответ (если есть свободные):**
```
📊 CS2: 3/5 аккаунтов свободно
```

**Ожидаемый ответ (если все заняты):**
```
📊 CS2: 0/5 аккаунтов свободно
⏰ Ближайший освободится через: 1ч 30мин
```

**Что проверяем:**
- [ ] Поиск игры по алиасу (пишешь `cs2`, `cs`, `counter` → находит)
- [ ] Количество верное
- [ ] Время ближайшего освобождения корректное
- [ ] Несуществующая игра → `"Игра не найдена"`

### 5.4 Неизвестная команда

**Что делаем:** покупатель пишет `!хуйня`.

**Что проверяем:**
- [ ] Система **не отвечает** на неизвестные команды (тишина)
- [ ] Обычные сообщения (без `!`) тоже не вызывают ответа

---

## 6. Тест: истечение аренды

### 6.1 Подготовка

Для быстрого теста создайте lot mapping с `rent_minutes: 5` (5 минут) и купите лот.

### 6.2 Ожидаемая цепочка

```
1. Время аренды истекло (end_time < now)
      ↓
2. Scheduler обнаруживает expired rental
      ↓
3. release_account():
   ├─ Если change_password_on_rent = true → СМЕНА ПАРОЛЯ в Steam
   │   (12-шаговый процесс через Steam Help)
   ├─ Если kick_devices_on_rent = true → КИК ВСЕХ СЕССИЙ
   └─ Steam-аккаунт → status: FREE
      ↓
4. Rental → status: EXPIRED
      ↓
5. Покупателю в чат FunPay:
```

**Сообщение покупателю (3 части, условные):**
```
⏰ Ваша аренда CS2 завершена!
Аккаунт: steam_user42

📦 Пожалуйста, зайдите в Покупки на FunPay и подтвердите заказ.

⭐ Будем благодарны за отзыв! Оставьте отзыв к заказу — получите бонусные часы при следующей аренде.
```

> Часть про "подтвердите заказ" и "отзыв" отправляется **только если покупатель ещё не подтвердил / не оставил отзыв**. Система запрашивает `get_order` чтобы проверить.

### 6.3 Что проверяем

**В логах:**
- [ ] `[steam_rent] Rental RENTAL_UUID expired for buyer BUYER_ID`
- [ ] `[steam_rent] Password changed for steam_user42` (если включено)
- [ ] `[steam_rent] Sessions kicked for steam_user42` (если включено)
- [ ] `[steam_rent] Account steam_user42 released → FREE`

**В чате FunPay покупателя:**
- [ ] Сообщение об окончании аренды пришло
- [ ] Просьба подтвердить заказ (если не подтверждён)
- [ ] Просьба оставить отзыв (если не оставлен)

**В UI Opium:**
- [ ] Rental → status: `EXPIRED`
- [ ] Steam Account → status: `FREE`

**В Steam:**
- [ ] Покупатель **не может** войти со старым паролем (если change_password = true)
- [ ] Все сессии покупателя завершены (если kick_devices = true)

---

## 7. Тест: отзыв и бонусные часы

> Система динамически добавляет/убирает бонусное время на основании отзыва покупателя.

### 7.1 Подготовка

- Lot mapping с `bonus_minutes: 60`, `min_rating_for_bonus: 4`
- Активная аренда у покупателя

### 7.2 Сценарий A: покупатель оставляет хороший отзыв (4-5 звёзд)

**Что делаем:** покупатель оставляет отзыв ≥ 4 звёзды к заказу.

**Ожидаемая цепочка:**
```
1. FunPay → системное сообщение "Покупатель X написал отзыв к заказу #ABCD1234"
      ↓
2. Система создаёт PendingReview (order_id, rental_id)
      ↓
3. Scheduler вызывает get_order(order_id) → получает количество звёзд
      ↓
4. Звёзды ≥ 4 → add_bonus_minutes(60) → end_time увеличивается на 1 час
      ↓
5. Покупателю сообщение: "⭐ Спасибо за отзыв! +1ч бонус!"
```

**Что проверяем:**
- [ ] Бонус добавлен: `end_time` увеличился на 60 минут
- [ ] Покупатель получил уведомление о бонусе
- [ ] В Rentals → `bonus_minutes: 60`
- [ ] Покупатель видит новое время через `!данные`

### 7.3 Сценарий B: покупатель изменяет отзыв (5→1 звезда)

**Что делаем:** покупатель редактирует отзыв, ставит 1 звезду.

**Ожидаемая цепочка:**
```
1. FunPay → системное сообщение "Покупатель изменил отзыв к заказу #ABCD1234"
      ↓
2. PendingReview (type: FEEDBACK_CHANGED)
      ↓
3. get_order → stars = 1 (< 4)
      ↓
4. Бонус был 60 мин → remove_bonus_minutes() → end_time уменьшается на 1 час
```

**Что проверяем:**
- [ ] Бонус **убран**: `end_time` сокращён
- [ ] `bonus_minutes: 0` в Rental
- [ ] Если `end_time` оказался в прошлом → аренда завершается немедленно

### 7.4 Сценарий C: покупатель удаляет отзыв

**Что делаем:** покупатель удаляет отзыв.

**Что проверяем:**
- [ ] Бонус убирается немедленно (без запроса `get_order`)
- [ ] `bonus_minutes: 0`

### 7.5 Edge: покупатель меняет отзыв 1→5→1→5

**Что проверяем:**
- [ ] Бонус добавляется → убирается → добавляется → убирается
- [ ] Система корректно отслеживает каждое изменение
- [ ] `end_time` правильно пересчитывается каждый раз

---

## 8. Тест: повторная покупка

> Покупатель покупает тот же лот, пока у него уже есть активная аренда той же игры.

### 8.1 Действие

**Что делаем:** покупатель покупает `"CS2 аренда 2 часа"` ещё раз (аренда CS2 уже активна).

### 8.2 Ожидаемая цепочка

```
1. NewOrderEvent → handle_new_order()
      ↓
2. Система находит: у buyer уже есть активная аренда CS2
      ↓
3. Вместо выдачи → создаёт PendingOrder
      ↓
4. При получении любого сообщения от покупателя → отправляет выбор:
```

**Сообщение покупателю:**
```
⚠️ У вас уже есть активная аренда CS2!
Логин: steam_user42
Осталось: 1ч 30м

Выберите действие:
!аренда CS2 — получить НОВЫЙ аккаунт
!продлить steam_user42 — продлить текущую аренду
```

### 8.3 Тест команды `!продлить`

**Что делаем:** покупатель пишет `!продлить steam_user42`.

**Что проверяем:**
- [ ] Текущая аренда продлена на `rent_minutes` из нового заказа
- [ ] `end_time` увеличился
- [ ] PendingOrder удалён
- [ ] Покупатель получил подтверждение: новый `end_time`
- [ ] Новый Steam-аккаунт **не выдан**

### 8.4 Тест команды `!аренда`

**Что делаем:** покупатель пишет `!аренда CS2`.

**Что проверяем:**
- [ ] Выдаётся **новый** Steam-аккаунт (другой, не тот что уже арендован)
- [ ] Покупатель получает логин/пароль нового аккаунта
- [ ] PendingOrder удалён
- [ ] Теперь у покупателя 2 активных аренды (`!данные` показывает обе)

---

## 9. Тест: все аккаунты заняты

### 9.1 Подготовка

Замораживаем или сдаём все Steam-аккаунты кроме одного. Сдаём последний.

### 9.2 Действие

**Что делаем:** покупатель покупает лот, но свободных аккаунтов нет.

### 9.3 Ожидаемая цепочка

```
1. NewOrderEvent → handle_new_order()
      ↓
2. find_free_account(game_id) → None (нет свободных)
      ↓
3. Создаёт PendingOrder
      ↓
4. При получении сообщения → уведомление:
```

**Сообщение покупателю:**
```
❌ Все аккаунты CS2 сейчас заняты.
⏰ Ближайший освободится через: 1ч 30мин

Что можно сделать:
!аренда CS2 — попробовать позже (когда освободится)
!возврат — оформить возврат средств
```

### 9.4 Тест: ожидание и повторная попытка

**Что делаем:** ждём пока один аккаунт освободится, покупатель пишет `!аренда CS2`.

**Что проверяем:**
- [ ] Аккаунт выделяется из только что освободившегося
- [ ] Покупатель получает данные

### 9.5 Тест: возврат

**Что делаем:** покупатель пишет `!возврат`.

**Что проверяем:**
- [ ] Деньги возвращены через FunPay (REFUND команда)
- [ ] PendingOrder удалён
- [ ] На FunPay: заказ в статусе "Возвращён"

---

## 10. Тест: возврат средств

### 10.1 Возврат через команду покупателя

**Предусловие:** у покупателя есть PendingOrder (не активная аренда!).

**Что делаем:** покупатель пишет `!возврат`.

**Что проверяем:**
- [ ] Все pending orders покупателя → REFUND
- [ ] Покупатель получает подтверждение
- [ ] Заказ на FunPay → статус "Returned"

### 10.2 Возврат через UI Opium

**Что делаем:** Orders → выбираем заказ → Refund.

**Что проверяем:**
- [ ] Возврат выполнен
- [ ] Статус заказа изменился

### 10.3 Что НЕ возвращается

- [ ] Активная аренда — `!возврат` **не** отменяет активную аренду
- [ ] `!возврат` работает только с PendingOrders

---

## 11. Тест: заморозка

### 11.1 Заморозка игры

**Что делаем:** Steam Rent → Games → кнопка Freeze у игры CS2.

**Что проверяем:**
- [ ] Игра помечена как `frozen`
- [ ] Новые заказы на CS2 → не обрабатываются (тихо пропускаются)
- [ ] **Существующие** активные аренды CS2 → продолжают работать до окончания
- [ ] Повторный Freeze → разморозка → заказы снова обрабатываются

### 11.2 Заморозка Steam-аккаунта

**Что делаем:** Steam Rent → Steam Accounts → Freeze у конкретного аккаунта.

**Что проверяем:**
- [ ] Аккаунт пропускается при поиске свободных
- [ ] Если это единственный аккаунт для игры → "все заняты"
- [ ] Существующая аренда на этом аккаунте → работает до окончания
- [ ] Повторный Freeze → разморозка

---

## 12. Тест: смена пароля и кик сессий

### 12.1 Автоматическая смена пароля (при завершении аренды)

**Предусловие:** `change_password_on_rent: true` у Steam-аккаунта.

**Что проверяем при завершении аренды:**
- [ ] Пароль изменён в Steam (12-шаговый процесс через Steam Help)
- [ ] Новый пароль сохранён в хранилище
- [ ] Старый пароль в `password_history` (максимум 5 записей)
- [ ] Пароль — 12 случайных символов (без неоднозначных: O/0, l/1, I)
- [ ] Новый пароль не совпадает ни с одним из последних 5

**В логах:**
- [ ] `[steam_rent] Start password change for steam_user42`
- [ ] Шаги: RSA login → recovery → mobile confirmation → set password
- [ ] `[steam_rent] Password changed successfully for steam_user42`

### 12.2 Ручная смена пароля (через UI)

**Что делаем:** Steam Accounts → Change Password.

**Что проверяем:**
- [ ] Пароль изменён
- [ ] Новый пароль отображается через "Show Password"

### 12.3 Кик сессий

**Что делаем:** Steam Accounts → Kick Sessions (или автоматически при завершении аренды).

**Что проверяем:**
- [ ] Все активные сессии Steam завершены
- [ ] Покупатель выброшен из Steam-клиента
- [ ] Логи: `[steam_rent] Sessions kicked for steam_user42`

### 12.4 Сбой смены пароля

**Что делаем:** ставим невалидный `identity_secret` и дожидаемся завершения аренды.

**Что проверяем:**
- [ ] Ошибка в логах (mobile confirmation failed)
- [ ] Аренда всё равно завершается
- [ ] Аккаунт всё равно освобождается (status → FREE)
- [ ] **Пароль НЕ изменён** — старый пароль по-прежнему рабочий
- [ ] Система не падает

---

## 13. Тест: авто-поднятие лотов

> Auto Raise поднимает все лоты на FunPay автоматически, контролируя кулдауны от FunPay.

### 13.1 Подготовка

- [ ] Модуль `auto_raise` подключён к аккаунту
- [ ] Конфигурация: `enabled: true`

### 13.2 Как работает

```
1. Модуль парсит профиль FunPay → находит все категории с лотами
      ↓
2. Для каждой категории отправляет RAISE_LOTS
      ↓
3. После успешного поднятия — сразу отправляет ещё одно (probe)
   чтобы узнать реальный кулдаун FunPay из ответа ошибки
      ↓
4. Устанавливает next_raise = now + cooldown + jitter
      ↓
5. Категории кэшируются на 6 часов
```

### 13.3 Тест автоматического поднятия

**Что делаем:** включаем `enabled: true`, ждём.

**Что проверяем:**
- [ ] Лоты поднимаются (видно на FunPay — лоты вверху списка)
- [ ] Логи: `[auto_raise] Raised category "Аренда Steam" successfully`
- [ ] GET .../status → `next_raise_at` показывает время следующего поднятия
- [ ] После кулдауна → автоматически поднимает снова

### 13.4 Тест ручного поднятия

**Что делаем:** нажимаем "Raise Now" в UI (или POST .../raise).

**Что проверяем:**
- [ ] Все кулдауны сброшены
- [ ] Категории перечитываются (кэш обновлён)
- [ ] Лоты поднимаются немедленно
- [ ] Результат в ответе: какие категории поднялись, какие ещё в кулдауне от FunPay

### 13.5 Тест кулдауна FunPay

**Что делаем:** поднимаем вручную 2 раза подряд.

**Что проверяем:**
- [ ] Второй раз → FunPay возвращает ошибку "подождите"
- [ ] Система парсит `wait_time` из ответа
- [ ] `next_raise_at` корректно рассчитан
- [ ] Лог: `[auto_raise] Category "X" in cooldown, next raise in Nmin`

### 13.6 Тест jitter (delay_range_minutes)

**Что делаем:** устанавливаем `delay_range_minutes: 5`.

**Что проверяем:**
- [ ] К каждому кулдауну добавляется случайный разброс 0-5 минут
- [ ] `next_raise_at` разных категорий — разные

### 13.7 Лог поднятий

**Что делаем:** GET .../log.

**Что проверяем:**
- [ ] Каждая запись: `timestamp`, `category_id`, `category_name`, `success`, `error`
- [ ] Максимум 300 записей (ротация)
- [ ] DELETE .../log → очищает

---

## 14. Тест: Telegram-бот — уведомления

> Telegram-бот отправляет уведомления о событиях FunPay в Telegram.

### 14.1 Подготовка

1. Создайте бота через @BotFather → получите токен
2. В UI: Telegram Bot → Settings → вставьте токен
3. Напишите боту `/start` в Telegram → получите свой Telegram ID
4. В UI: Telegram Bot → Whitelist → добавьте свой ID

### 14.2 Тест: уведомление о новом заказе

**Что делаем:** покупатель оплачивает лот.

**Ожидаемое сообщение в Telegram:**
```
🛒 [account_id] Новый заказ

Order: #ABCD1234
Описание: CS2 аренда 2 часа
Покупатель: BuyerName
Сумма: 150₽
Статус: paid
```

**Что проверяем:**
- [ ] Сообщение пришло в Telegram
- [ ] Формат HTML (жирный текст и т.д.)
- [ ] Содержит order_id, описание, покупатель, сумму

### 14.3 Тест: уведомление о новом сообщении

**Что делаем:** покупатель пишет "Привет!" в чат FunPay.

**Ожидаемое сообщение в Telegram:**
```
💬 [account_id] Новое сообщение

BuyerName: Привет!
```

**Что проверяем:**
- [ ] Сообщение пришло
- [ ] **Свои** сообщения (от бота/системы) — **НЕ** пересылаются
- [ ] Длинные сообщения обрезаются до 500 символов
- [ ] Картинки → `[изображение]`

### 14.4 Тест: фильтрация событий

**Что делаем:** в конфигурации отключаем `new_message` из `notify_events`.

**Что проверяем:**
- [ ] Уведомления о заказах приходят
- [ ] Уведомления о сообщениях **НЕ** приходят
- [ ] Включаем обратно → приходят

### 14.5 Тест: тестовое сообщение

**Что делаем:** в UI нажимаем "Test" (или POST .../test).

**Что проверяем:**
- [ ] Все пользователи whitelist получили тестовое сообщение
- [ ] Если whitelist пусой → предупреждение

---

## 15. Тест: Telegram-бот — команды и кнопки

### 15.1 Команда `/start` (от незнакомого пользователя)

**Что делаем:** человек НЕ из whitelist пишет `/start` боту.

**Ожидаемый ответ:**
```
Ваш Telegram ID: 123456789
Попросите администратора добавить вас в whitelist.
```

**Что проверяем:**
- [ ] Бот отвечает с Telegram ID
- [ ] Этот ID можно добавить в whitelist
- [ ] Другие команды от этого пользователя → **тишина** (игнорируются)

### 15.2 Команда `/start` (от whitelisted)

**Что проверяем:**
- [ ] Приветственное сообщение
- [ ] Список доступных команд

### 15.3 Команда `/status`

**Что проверяем:**
- [ ] Account ID
- [ ] Бот @username
- [ ] Количество пользователей в whitelist
- [ ] Количество событий в логе
- [ ] Подписки на типы событий

### 15.4 Команда `/events`

**Что проверяем:**
- [ ] Последние 10 событий
- [ ] Каждое: время, тип, кол-во доставок

### 15.5 Команда `/menu` — Inline кнопки

**Предусловие:** добавлены bot buttons в UI.

**Пример кнопки:**
```json
{
  "label": "📊 Статус аренд",
  "api_endpoint": "/api/accounts/{account_id}/modules/steam_rent/overview",
  "api_method": "GET",
  "confirm": false
}
```

**Что делаем:** отправляем `/menu` → нажимаем кнопку.

**Что проверяем:**
- [ ] Кнопки отображаются (2 в ряд)
- [ ] Нажатие → бот делает HTTP-запрос к API
- [ ] Результат отображается в Telegram (`✅ + JSON данные`)
- [ ] Ошибка API → `❌ + код ошибки + detail`

### 15.6 Кнопка с подтверждением

**Предусловие:** кнопка с `confirm: true`.

**Что проверяем:**
- [ ] При нажатии → `"⚠️ Подтвердите: {label}"` + кнопки Yes/Cancel
- [ ] Cancel → `"❌ Отменено"`, действие не выполнено
- [ ] Yes → действие выполнено

### 15.7 Кнопка "Ручной raise"

**Пример:**
```json
{
  "label": "🔝 Поднять лоты",
  "api_endpoint": "/api/accounts/{account_id}/modules/auto_raise/raise",
  "api_method": "POST",
  "confirm": true
}
```

**Что проверяем:**
- [ ] Кнопка → подтверждение → лоты поднимаются
- [ ] Результат: какие категории поднялись

---

## 16. Тест: Log Watchers

> Log Watcher — мониторинг логов системы с пересылкой в Telegram.

### 16.1 Настройка

**Что делаем:** создаём log watcher в UI (Telegram Bot → Log Watchers):

```json
{
  "pattern": "ERROR",
  "custom_message": "",
  "enabled": true
}
```

`pattern` — **подстрока** (не regex). Ищется в отформатированной строке лога.

### 16.2 Тест: появление ошибки в системе

**Что делаем:** провоцируем ошибку (например, невалидный `golden_key` при создании аккаунта).

**Ожидаемое в Telegram:**
```
<pre>2026-02-16 12:34:56 | ERROR | opium.core | Failed to initialize account test: Invalid golden_key</pre>
```

**Что проверяем:**
- [ ] Сообщение пришло в Telegram
- [ ] Формат: `<pre>полная строка лога</pre>`
- [ ] Только ошибки (DEBUG/INFO — не пересылаются)

### 16.3 Тест: кастомное сообщение

**Что делаем:** создаём watcher с `pattern: "Password changed"`, `custom_message: "🔑 Пароль изменён!"`.

**Что проверяем:**
- [ ] При появлении в логах "Password changed" → в Telegram: `"🔑 Пароль изменён!"`
- [ ] Не полная строка лога, а наш текст

### 16.4 Анти-рекурсия

**Что проверяем:**
- [ ] Логи самого telegram_bot (`opium.telegram_bot.*`) **НЕ** триггерят watchers
- [ ] Иначе бесконечный цикл: watcher → сообщение → лог → watcher → ...

---

## 17. Тест: шаблоны сообщений

> Все сообщения покупателям настраиваемые через Messages в UI.

### 17.1 Просмотр шаблонов

**Что делаем:** Steam Rent → Messages.

**Что проверяем:**
- [ ] 24 шаблона сгруппированы по категориям (status, account, delivery, expiry...)
- [ ] Каждый шаблон показывает доступные плейсхолдеры

### 17.2 Редактирование шаблона

**Что делаем:** изменяем шаблон `rent_success` (выдача данных).

**Что проверяем:**
- [ ] Изменения сохранены
- [ ] При следующей выдаче → используется новый шаблон
- [ ] Плейсхолдеры `{login}`, `{password}`, `{guard_code}` подставляются

### 17.3 Условные строки

Если плейсхолдер в строке пустой, **вся строка** убирается из сообщения.

**Пример:**
```
🎮 Аренда оформлена!
Логин: {login}
Пароль: {password}
Steam Guard: {guard_code}
```

Если `guard_code` пустой (нет `.maFile`):
```
🎮 Аренда оформлена!
Логин: steam_user42
Пароль: Ab3kQ7mN9xZw
```

**Что проверяем:**
- [ ] Строка "Steam Guard:" убирается когда нет `.maFile`
- [ ] Остальные строки на месте

---

## 18. Тест: прокси

### 18.1 Добавление прокси

**Что делаем:** Steam Rent → Proxies → Add.

**Что проверяем:**
- [ ] Прокси создан
- [ ] Check → показывает IP, latency

### 18.2 Назначение прокси на Steam-аккаунт / игру

**Что проверяем:**
- [ ] Операции со Steam (смена пароля, кик) проходят через прокси
- [ ] Логи: `Using proxy socks5://...`

### 18.3 Прокси-листы

**Что проверяем:**
- [ ] Можно группировать прокси в листы
- [ ] Назначить лист на игру/аккаунт
- [ ] Ротация прокси из листа

---

## 19. Тест: ручное управление арендами

### 19.1 Продление аренды (через UI)

**Что делаем:** Rentals → выбираем аренду → Add Time → +2 hours.

**Что проверяем:**
- [ ] `end_time` увеличен на 2 часа
- [ ] Покупатель видит новое время через `!данные`

### 19.2 Принудительное завершение (через UI)

**Что делаем:** Rentals → Terminate.

**Что проверяем:**
- [ ] Аренда завершена (status → REVOKED, не EXPIRED)
- [ ] Пароль сменён (если настроено)
- [ ] Сессии кикнуты (если настроено)
- [ ] Аккаунт → FREE
- [ ] Покупатель получил уведомление

### 19.3 Сокращение аренды

**Что делаем:** Rentals → Add Time → -1 hour (отрицательное значение).

**Что проверяем:**
- [ ] `end_time` уменьшен
- [ ] Если `end_time` < now → аренда завершается немедленно

---

## 20. Тест: предупреждение об истечении

### 20.1 Настройка

**Что делаем:** config → `expiry_warning_minutes: 5`.

### 20.2 Тест

**Что делаем:** аренда осталось < 5 минут.

**Ожидаемое сообщение покупателю:**
```
⏰ До конца аренды CS2 осталось 5мин!
Логин: steam_user42
Для продления оплатите лот и напишите: !продлить steam_user42
```

**Что проверяем:**
- [ ] Предупреждение пришло **один раз** (не спамит)
- [ ] `warning_sent: true` в Rental
- [ ] Содержит инструкцию по продлению
- [ ] Если `expiry_warning_minutes: 0` → предупреждение отключено

---

## 21. Edge-кейсы

### 21.1 Заказ не матчится на lot mapping

**Что делаем:** покупатель покупает лот, название которого не совпадает ни с одним `lot_pattern`.

**Что проверяем:**
- [ ] Заказ тихо пропускается (нет ошибки)
- [ ] В логах: DEBUG-уровень запись
- [ ] Система продолжает работать

### 21.2 Steam-аккаунт без .maFile

**Что делаем:** аренда на аккаунте без `shared_secret`.

**Что проверяем:**
- [ ] Выдача работает (логин + пароль)
- [ ] Guard-код → пустой / `—`
- [ ] Строка "Steam Guard:" убирается из сообщения
- [ ] Команда `!код` → `"—"` для этого аккаунта
- [ ] Смена пароля **не** работает (нужен `.maFile`)

### 21.3 Покупатель не пишет в чат (delivery_pending)

**Что делаем:** покупатель купил лот но ничего не писал.

**Что проверяем:**
- [ ] Rental создан с `delivery_pending: true`
- [ ] Системное сообщение от FunPay о покупке триггерит доставку
- [ ] Если даже системного нет — данные доставятся при **любом** следующем сообщении

### 21.4 Потеря соединения с FunPay

**Что делаем:** временно ломаем интернет / ставим невалидный proxy.

**Что проверяем:**
- [ ] Логи: warning о потере соединения
- [ ] Reconnect пытается восстановить
- [ ] Аренды **не теряются** (хранятся в JSON)
- [ ] После восстановления — всё работает
- [ ] PendingReviews не теряются (обрабатываются при восстановлении)

### 21.5 Перезапуск сервера во время активных аренд

**Что делаем:** `stop.bat` → `start.bat` пока есть активные аренды.

**Что проверяем:**
- [ ] Аренды загружаются из `rentals.json`
- [ ] Scheduler продолжает отслеживать истечения
- [ ] `delivery_pending` аренды доставятся при следующем сообщении
- [ ] PendingOrders не потеряны
- [ ] Telegram бот переподключается

### 21.6 Два заказа от одного покупателя одновременно

**Что делаем:** покупатель быстро покупает 2 разных лота (CS2 + Dota2).

**Что проверяем:**
- [ ] Каждый заказ обработан отдельно
- [ ] Выданы 2 разных Steam-аккаунта
- [ ] `!данные` показывает обе аренды
- [ ] Нет race condition / дублирования

### 21.7 Дубликат заказа (NewOrderEvent пришёл дважды)

**Что проверяем:**
- [ ] Dedup по `order_id` — второй event игнорируется
- [ ] Логи: `Order ORDER_ID already processed, skipping`

### 21.8 PendingOrder протух (TTL)

**Предусловие:** `pending_ttl_minutes: 1440` (24 часа).

**Что делаем:** покупатель не выбрал действие за 24 часа.

**Что проверяем:**
- [ ] Scheduler удаляет протухший PendingOrder
- [ ] Логи: `Cleaned up stale pending order for buyer BUYER_ID`

---

## 22. Чеклист полного прогона

### Базовая работа

| # | Проверка | Статус |
|---|---------|--------|
| 1 | Система запущена, аккаунт running | ☐ |
| 2 | Lot mappings настроены под лоты FunPay | ☐ |
| 3 | Steam-аккаунты добавлены, .maFile импортирован | ☐ |
| 4 | Guard-код генерируется | ☐ |

### Аренда (основной флоу)

| # | Проверка | Статус |
|---|---------|--------|
| 5 | Покупка лота → заказ обработан, аккаунт выдан | ☐ |
| 6 | Покупатель получил логин + пароль + guard в чате | ☐ |
| 7 | `!данные` → показывает аренду | ☐ |
| 8 | `!код` → показывает guard-код | ☐ |
| 9 | `!статус` → показывает свободные аккаунты | ☐ |
| 10 | Истечение аренды → пароль сменён, сессии кикнуты | ☐ |
| 11 | Покупателю пришло уведомление о завершении | ☐ |
| 12 | Аккаунт освободился → status: FREE | ☐ |

### Отзыв + бонус

| # | Проверка | Статус |
|---|---------|--------|
| 13 | Отзыв ≥ 4 звезды → бонус добавлен | ☐ |
| 14 | Отзыв изменён < 4 → бонус убран | ☐ |
| 15 | Отзыв удалён → бонус убран | ☐ |

### Конфликты

| # | Проверка | Статус |
|---|---------|--------|
| 16 | Повторная покупка → выбор продлить / новый аккаунт | ☐ |
| 17 | Все аккаунты заняты → ожидание или возврат | ☐ |
| 18 | `!возврат` → деньги вернулись | ☐ |

### Авто-поднятие

| # | Проверка | Статус |
|---|---------|--------|
| 19 | Лоты поднимаются автоматически | ☐ |
| 20 | Кулдауны FunPay соблюдаются | ☐ |
| 21 | Ручное поднятие работает | ☐ |

### Telegram

| # | Проверка | Статус |
|---|---------|--------|
| 22 | Уведомление о заказе пришло в Telegram | ☐ |
| 23 | Уведомление о сообщении пришло | ☐ |
| 24 | `/menu` → кнопки работают | ☐ |
| 25 | Log watcher → ошибки пересылаются | ☐ |

### Устойчивость

| # | Проверка | Статус |
|---|---------|--------|
| 26 | Перезапуск → аренды сохранены | ☐ |
| 27 | Потеря интернета → reconnect работает | ☐ |
| 28 | Ошибка смены пароля → система не падает | ☐ |
